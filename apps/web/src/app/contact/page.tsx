import type { Metadata } from "next";
export const metadata: Metadata = { title: "Contact", description: "Start a serious conversation with Flective." };
export default function ContactPage() { return <section className="section"><div className="shell contact-layout"><div className="contact-copy"><p className="eyebrow">Start a project</p><h1>What needs a clearer view?</h1><p>Tell us what you are trying to solve. We will start with the business question, not a prepackaged answer.</p></div><form className="contact-form" action="#" method="post"><div className="field"><label htmlFor="name">Name</label><input id="name" name="name" autoComplete="name" required /></div><div className="field"><label htmlFor="email">Email</label><input id="email" name="email" type="email" autoComplete="email" required /></div><div className="field"><label htmlFor="company">Company</label><input id="company" name="company" autoComplete="organization" /></div><div className="field"><label htmlFor="challenge">What are you trying to solve?</label><textarea id="challenge" name="challenge" required /></div><div className="field"><label htmlFor="help">What can we help with?</label><select id="help" name="help" defaultValue=""><option value="" disabled>Select an area</option><option>Intelligence</option><option>Growth</option><option>Build</option><option>A connected engagement</option></select></div><div className="field"><label htmlFor="budget">Budget range (optional)</label><input id="budget" name="budget" placeholder="e.g. IDR 50–100M" /></div><div className="field"><label htmlFor="timeline">Timeline (optional)</label><input id="timeline" name="timeline" placeholder="e.g. Starting in Q3" /></div><button className="button" type="submit">Send your brief <span>→</span></button><p className="form-note">This form is a front-end structure only. It is not connected to the existing automation workflow.</p></form></div></section> }
